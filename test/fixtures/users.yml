<% SALT = "NaCl" unless defined?(SALT) %>

one:
  name: katie
  hashed_password: "<%= User.encrypt_password('private', SALT) %>"
  salt: <%= SALT %>

two:
  name: MyString
  hashed_password: MyString
  salt: MyString
